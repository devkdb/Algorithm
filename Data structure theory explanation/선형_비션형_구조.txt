
// 선형 구조는 자료를 순차적으로 나열한 형태
- 배열
- 연결 리스트
- 스택 / 큐

// 비선형 구조는 하나의 자료 뒤에 다수의 자료가 올 수 있는 형태
- 트리
-그래프

// 배열, 동적 배열, 연결 리스트 비교

// 동적 배열 할당 정책:
- 실제 사용할 방보다 많이, 여유분을 두고 예약.
- 이사 횟수를 최소화.
- 단점: 중간 삽입/삭제

// 연결 리스트
- 단점: N번째 방을 바로 찾을 수가 없음(임의 접근 Random Access 불가)